{
  "name": "pickleball-motion-analyzer",
  "version": "1.0.0",
  "private": true,
  "description": "Public frontend for Pickleball Motion Analyzer.",
  "scripts": {
    "build": "node scripts/prepare-vercel.js",
    "build:pages": "node scripts/prepare-github-pages.js",
    "prepare:hf": "node scripts/prepare-hf-space.js",
    "preview": "node scripts/prepare-vercel.js && npx serve dist"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "@mediapipe/tasks-vision": "^0.10.18"
  }
}
